# LabAllCompass Master Architecture Blueprint — R385

## Authority separation
Canonical scientific inventory is distinct from the control plane. Control-plane restoration never creates Registry456/Product39 by itself.

Authority ladder: `CURRENT_CANONICAL -> CERTIFIED_CONTROL_PLANE -> SHADOW_CAPABILITY -> EXPERIMENTAL -> EVIDENCE_ONLY`.

## Entity taxonomy — do not collapse these
- **Registry455** — scientific mechanism-family ontology.
- **LCB kernels: 96** — reusable executable kernels.
- **Foundry parents: 69** — reusable parent capabilities with provenance/interfaces.
- **Foundry executable compositions: 145** — working composition modules/capabilities; they are not automatically distinct products.
- **Canonical capability inventory: 310 = 96 + 69 + 145.**
- **V2 Products38** — 38 completed executable composition suites on completed interaction edges.
- **Quality28** — reporting-level distinct product families after anti-iteration-inflation collapse; not 28 additional executable entities.
- **Operational entity ledger: 348 = 310 capabilities + 38 V2 suites.**

R384's 202-row genealogy is superseded. It mixed 38 V2 suites + 145 Foundry products + only 19 Foundry parents, while omitting the other 50 parents and all 96 LCB kernels. The exact old ledger is preserved under `11_SOURCE_ARCHIVE/R385_ENTITY_AND_INSTRUMENTATION_RESTORATION/`.

## Operational architecture
1. **Canonical inventory** — Registry455, 4000 primitives, 310 capabilities, Products38, Quality28.
2. **Discovery core** — structured multi-view retrieval, capability/primitive donor retrieval.
3. **Typed equivalence lattice** — proposal identity, motif equivalence, full-mechanism equivalence, product-family equivalence remain distinct.
4. **Interaction & instrumentation map** — typed supplier/consumer/parent/rejected edges; experiment contracts and telemetry are attached to exact edges rather than kept as free-floating logs.
5. **Failure inventory / research router** — unresolved claim boundaries and missing operators from current Lab state.
6. **Foundry/product selector** — native historical queue/selection mechanisms; no hand-picked frontier by default.
7. **Experiment selector** — choose the experiment that resolves the current decision per cost/irreversibility.
8. **Repair & verification lane** — invariant-authorized repair, AST/operator synthesis, descendants, MCES, fail-closed vs unsafe-wrong typing.
9. **Specification/assurance lane** — verifier independence, provenance DAG, frozen blind challenges, preserved escapes.
10. **Claim/obligation transport graph** — value + semantic type/version + scope + assumptions + provenance + dependencies + certificates.
11. **Numerical/structural soundness** — interval/exact escalation, fixed-point validity, composition-law defects.
12. **Proof-carrying pipeline** — runtime claim envelopes and semantic fingerprints.
13. **Freshness/runtime governance** — evidence dependency freshness, effective roots, explicit preconditions, authority monotonicity.
14. **Impossibility/closure gate** — SOLVABLE/PARTIAL/IMPOSSIBLE/UNKNOWN before expensive search when possible.
15. **Shadow promotion pipeline** — idea -> experiment -> adapter -> shadow prototype -> shadow capability -> product candidate -> quality candidate -> canonical.
16. **Case library** — R181+ is searchable evidence, not automatic frontier authority.

## Native R12 instrumentation loop
`interaction edge -> declared experiment contract -> run -> telemetry event -> index by edge/product/shell/test -> conservative exact-shell calibration -> hydrate edge/Foundry queue -> next experiment/product decision`.

At R12: 4310 nodes = 4000 primitive + 310 capability; 1057 edges = 38 completed + 962 inferred + 53 parent-of + 4 rejected. The telemetry ledger has 316 events, all matched, covering 38 completed edges / 38 products / 38 shells / 38 tests, with 38 exact-shell calibrated current suites.

Telemetry is therefore not merely a contract or logging facility. It is **placement-aware instrumentation on the interaction topology**.

## Normal research cycle
`current state -> unresolved failure inventory -> closure/impossibility -> multi-view retrieval -> Registry/capability retrieval -> interaction expansion -> typed quotient -> native selector -> collision/prior art -> obligation propagation -> declared experiment contract -> native experiment selector -> executable experiment -> telemetry placement/calibration -> Foundry hydration -> removal/ablation -> numerical/structural assurance -> blind assurance if needed -> freshness -> shadow gate -> quality gate -> canonical mutation only if explicitly justified`.


# R386 extension — Retroactive Productization Loop

R386 reopens productization without removing the R12 quality gates. Later-discovered unresolved failures are routed backward through the restored discovery core rather than choosing a manual frontier.

Canonical loop:

`unresolved failure inventory -> impossibility/closure screen -> structured multi-view retrieval -> Registry/capability retrieval -> typed interaction expansion -> typed collision/equivalence -> native Foundry ranking -> removal-control benchmark -> telemetry placement -> quality audit -> explicit promotion decision`

The 961-edge static disposition ledger is a triage layer only. It cannot certify equivalence or promotion. Executable suite accounting and distinct product-family accounting remain separate.

# R387–R393 architecture corrections and current authority

## Product authority is an explicit overlay, not a count rewrite

R12 remains the historical scientific base audit. R387–R392 use `CURRENT_PRODUCT_AUTHORITY.json` to promote tested executable/product-family overlays without mutating Registry455. Current product authority is R392: Products52 / Quality39.

## Visibility completeness is a first-class invariant

R388 established that anti-duplication logic can itself create blind spots. A provenance bucket (`FOUNDRY_PARENT`) must never be used as semantic-family identity unless equivalence is independently justified. The repaired generator exposes parent↔parent interactions while the normal quality gates still prevent trivial variants from becoming distinct families.

The persistent native candidate universe is **16,594 rows**. The top-1000 queue is a computationally bounded priority window only. Full-universe memory must survive every refresh and authority transition.

## Retro50 semantics

The Retro50 are 50 executable historical parents inside the 69 Foundry-parent capability records. They are a first-class composition reservoir, not 50 automatic V2 products and not 50 automatic Quality families. R388's repair exposed 890 previously hidden rank-compatible parent↔parent pairs.

## Executable-suite discovery is dynamic

Physical suite discovery and packaged direct regression must auto-discover all current `V2P*/test_*.py` surfaces. Required product artifacts such as `PRODUCT_RESULT.md` are part of discovery authority; missing required artifacts must produce visible incompleteness rather than silently dropping a suite.

## Telemetry authority

Only current exact-shell/current-signature mechanics telemetry calibrates current suites. Old successful pilots are superseded after source/test fingerprint changes. Mechanics telemetry is topology-bound but not empirical-learning evidence.

## Regression inheritance

A clean historical regression surface may be inherited only after byte-tree identity against the prior archive-of-record. New or changed bytes require fresh execution. Promotion bundles must record inherited hashes and fresh-test receipts separately.

## Historical closure semantics

Historical release tests preserve historical audit counts exactly, but they must not assert that later product authorities cannot exist. Current authority and historical audit are separate relations.

## Current normal research cycle

`R392 current authority -> R393 full-universe fresh frontier -> failure routing -> diversity clustering -> code-level interaction/ancestry adjudication -> non-additive operator -> nearest-family/prior-art collision -> mechanism-removing control -> working/collapse/failure/abstention regions -> shadow suite -> complete product artifacts -> direct tests -> fresh exact-shell mechanics telemetry -> candidate closure -> regression/byte-identity proof -> explicit product-family adjudication -> explicit authority promotion`.

**Count is not the optimization target. Complete visibility plus genuine mechanism distinctness is.**



# Final retirement architecture addendum — R394–R398

The control plane at retirement has a **dual fidelity boundary**. Logical product authority reached R397 (56 executable suites / 43 distinct families), while the last physically preserved exact worktree remains R392 under the R393 archive. R398 is a candidate overlay with V2P058 and must not be promoted from receipts alone.

Late product mining reinforced four architectural rules:

1. Semantic alignment is an explicit input/authority object. A convenient transform is not a bridge.
2. Mechanism removal is the primary product comparator; the new parent must alter the consumer before its decision, not decorate output.
3. Failure/collapse regions are first-class product metadata.
4. Negative knowledge and the full candidate universe are persistent memory; saturation is measured by increasing collision/invalid-bridge yield, not by a target product count.

See `19_R394_R398_FINAL_RETIREMENT_DELTA/` for the final additive layer and source-fidelity warning.
