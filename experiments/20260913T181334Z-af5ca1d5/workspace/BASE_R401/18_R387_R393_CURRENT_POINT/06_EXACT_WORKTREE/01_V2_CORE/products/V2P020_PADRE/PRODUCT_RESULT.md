# V2P020_PADRE — Privacy-Accuracy Decision-Relevant Explorer

Composition: `FOUNDRY:P092 -> PARENT:IM334_IM094_DRE`.

Mechanism: PDAT converts privacy noise into a decision-correctness gate before DRE uses a private measurement. Exploration proceeds only when the declared epsilon/margin shell clears the required action accuracy.

Mechanism-removing comparator: downstream consumer without the upstream gate/audit/tipping signal.

Claim boundary: deterministic synthetic executable contract only.
