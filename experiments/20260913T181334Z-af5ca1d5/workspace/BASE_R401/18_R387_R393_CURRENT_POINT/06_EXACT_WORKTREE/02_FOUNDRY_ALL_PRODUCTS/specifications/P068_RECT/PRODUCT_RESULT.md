# Relational-Evidence Calibrated Trigger

**Status:** PROMOTED v0.1

**Composition:** REL + DTTC

Turns REL marginal culprit posterior and raw discrepancy into competing trigger signals; DTTC selects and freezes the lower-cost escalation rule before validation.

## Validation

See `BENCHMARK_RESULT.json` and the dedicated unit tests. Claims are limited by the implementation contract.
