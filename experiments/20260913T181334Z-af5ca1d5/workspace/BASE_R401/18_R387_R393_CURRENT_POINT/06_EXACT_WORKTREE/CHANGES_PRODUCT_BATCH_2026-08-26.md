# Product-generation continuation after R12

This batch resumes product generation before the crypto campaign and leaves the Foundry source tree unchanged.

## V2P008 TGCARS

`LCB-K071 GuaranteeTransportGateV0 -> FOUNDRY:P135 CARS`. A deletion certificate may keep zero sensing consequence only when its supporting residual/conditional law passes a declared transport audit. Failed/unknown transport disables zeroing, restores non-zero declared consequence weights, and requires re-certification. 12/12 focused tests pass. In the deterministic shifted-shell benchmark, hidden-oracle loss changes from 0.8488 for blind P135 reuse to 0.1360 for the guarded allocation; this is synthetic mechanism evidence only.

## V2P009 SGMB

`FOUNDRY:P140 BOSA -> LCB-K048 MultiFidelityBudgetControllerV0`. Cheap-proxy allocation is permitted only while BOSA reports usable cohort support. At month 24 in the preserved burnout shell, ESS fraction is 0.661384 and a correlation-only K048 comparator allocates 115 cheap acquisitions; SGMB routes fine-only and makes unsupported cheap acquisitions zero. 12/12 focused tests pass.

## V2P010 SCSPIA

`PARENT:R037_SUPPORT_COVARIANCE_SACPS -> FOUNDRY:P138 SPIA`. Belief covariance is estimated under a declared support mask in orthonormal simplex-tangent coordinates, then lifted as `B C B^T` before SPIA prices measurement channels. In the fixed seed-1 small-sample benchmark, oracle chooses c6, raw sample covariance chooses c1, and SCSPIA recovers c6. Absolute oracle-channel value error drops from 0.0515576 to 0.00159053 in that synthetic shell. 12/12 focused tests pass.

## Composition identity repair

Every V2 product now carries `COMPOSITION.json`. Suite discovery reads explicit supplier/consumer IDs first and uses the legacy prose parser only as fallback. This removes the previous directional parser limitation and allows valid Foundry->LCB and Foundry-parent->Foundry composites to enter telemetry, campaign memory and the interaction map.

## Integrated state

- 310 base capability records.
- 10 completed V2 composites; 320 total addressable artifacts.
- 108 standardized mechanics events; 108/108 exact current-suite matches.
- 10/10 completed executable suites calibrated.
- Interaction map: 4,310 nodes / 1,057 typed edges: 10 completed compositions, 990 inferred candidates, 53 parent edges, 4 rejected-current-interface edges.
- Campaign memory: 10 mechanics passes + 4 interface rejections; empirical learning-eligible outcomes remain 0.
