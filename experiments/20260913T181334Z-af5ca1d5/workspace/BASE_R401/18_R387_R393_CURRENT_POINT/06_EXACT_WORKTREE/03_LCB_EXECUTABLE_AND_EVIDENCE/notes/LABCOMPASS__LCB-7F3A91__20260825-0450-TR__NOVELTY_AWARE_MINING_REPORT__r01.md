# LABCOMPASS LCB-7F3A91 — novelty-aware re-mining report r01

## Why this pass exists

The original 209-edge queue exhausted its unadjudicated `transfer=5/product=5` frontier after a direct-theory collision sweep. A fresh ordered pair surface was therefore generated from all 455 canonical families, excluding already-curated IM sets and penalizing same-theory lexical overlap.

## Prefilter

- Ordered cross-domain candidates scored: **131,088**
- Top candidates persisted: **1,000**
- Features: canonical primitive, exact donor concepts/shells, semantic tags, operator/certificate role, cross-domain separation, lexical/source-concept collision penalties.
- This remains a candidate generator, not an automated novelty claim.

## Manual top-50 adjudication

Status counts: `{'QUARANTINE_WEAK_BRIDGE': 15, 'DEMOTE_DIRECT_THEORY': 10, 'DEMOTE_PRIOR_ART': 8, 'HOLD_WEAK': 4, 'HOLD_DEEP_RESEARCH': 3, 'DEMOTE_EXISTING_CORE': 3, 'DEMOTE_WEAK_BRIDGE': 1, 'PROMOTED_RX200_CHILD': 1, 'DEMOTE_DIRECTIONAL_DUPLICATE': 1, 'HOLD_APPLIED': 1, 'DEMOTE_SAME_MECHANISM': 1, 'QUARANTINE_INTERACTION_MISMATCH': 1, 'DEMOTE_DUPLICATE': 1}`

The most important result is negative: high prefilter score still frequently connects mature neighboring theories or produces an interaction with no executable missing operator. Manual integrity adjudication remains mandatory.

### Promoted

`IM-088 + IM-247 -> IM-335` was promoted as **RX-200**, not as a new primitive. It adds a moving-boundary state to the already benchmarked harmonic-accessibility sensing layer. The dynamic extension survives, while stronger static prior art forces the parent RX-145 product score down.

### Deep-research holds

- `IM-311 -> IM-233`: rare/weak collective evidence as a possible identifiability/layer-mismatch gate for two-unknown-layer recovery. Targeted search found no direct higher-criticism/blind-deconvolution match, but the operational bridge is not yet precise enough to benchmark honestly.
- `IM-203 -> IM-284`: possible two-scale influence topology (vanishing pairwise/long-range signal while collective/local influence survives). Needs a concrete observable and receiving domain before compute.

### Rejected example

`IM-267 -> IM-238` (prophet bound -> clawback/delayed verification) was considered manually but not promoted. A prophet inequality bounds clairvoyance in a sequential stopping problem; retroactive verification of an earlier payoff is not automatically equivalent to clairvoyant access to future options. Without an extra decision model, the interaction would be analogy rather than mechanism.

## Binding next line

Do not revert to the old queue's 5/5 ranking. Continue from this novelty-aware prefilter with the order:

1. interaction-integrity test;
2. existing-core/ontology collision;
3. direct prior-art search;
4. only then destructive benchmark.

Priority unresolved candidate: `IM-311 -> IM-233`, but only after the layer-recovery mechanism is made explicit enough that the benchmark tests more than ordinary higher-criticism power.
