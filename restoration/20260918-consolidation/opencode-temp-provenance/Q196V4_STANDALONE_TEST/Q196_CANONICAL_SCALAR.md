# Q196 Canonical Scalar (§§6, 10, 16–18)

Θ_{d,n_0}(ξ) = λ⋆(Q^P_ξ), Q196_THETA_TABLE.txt (752 rows with λ⋆ defined;
10 zeros = exactly the V1 collinear cells). By order class: every
divisor order has passing witnesses (pair/lstar tables); zeros are
isolated full-order (mostly) frequencies.

- Fixed-width: (α,β) = preferred (z_{d+1},z_{d+2}) with observed
  universal fallback (z_{d+1},z_{r−1}); triangular M_Q196 with det 2B_Q.
- d mod 6: zeros show no mod-6 concentration beyond the order pattern
  (orders present: full r, plus 11/3 stragglers); no formula forced.
- Sectors (§19): S1 gives no ξ^d-narrowing (u^d = v^d parametrizes
  v = uθ^s with ξ = u²θ^s unconstrained; s = 0 likewise); all-ξ testing
  stands (stronger than needed, hence valid).
- Numerator (§18): no closed N(ξ)/D(ξ) split derived; Θ recorded raw
  per cell. The exact remaining obstruction (Part O): closed ξ-formulas
  for propagation/anchor coefficients.
