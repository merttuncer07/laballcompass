# Current checkpoint — 2026-09-15

The broad lab/product goal remains active. This checkpoint records completed work; it does not narrow the goal or establish product/field success.

- Current working directory: this `lab-restored` folder. Earlier ZIP and original Downloads archives remain preserved; the earlier ZIP does not contain subsequent workbench/product changes.
- Existing strategy: `../lab-strategy/LAB_STRATEGY.md`. The lab has 293 inventoried components; 89 Foundry products still use generic spec_runtime, 20 shared reference implementations, and P103/P083 have two new native reimplementations. Missing historical code has not been recovered by relabeling it.
- `PRODUCT_REPAIRS.md`: SCIG support/regularization contradictions and signed-dependence handling, BICC invalid-certificate/scenario semantics, native P103 HFAD → BICC → ACRA, and ACRA input repairs. 36 targeted tests passed; two ACRA consumers passed 20 further tests. Current proof: `runs/20260914T131915Z-product-repairs/receipt.json` and `restoration/20260914-product-repairs/acra-consumers.log`.
- P103 CLI: `.venv/bin/python lab.py flow-review examples/circular-flow/input.json`. Actual saved result: `examples/circular-flow/cli-result.json`. Hodge cycle-space energy is NOT proof of a directed payment cycle; the additive review score and option effectiveness remain model assumptions. This is engineering restoration, not demonstrated auditor value.
- `WORKBENCH.md`: Excel parsing now supports a supplied collection of up to 20 workbooks. Named and numeric external workbook references map only to uniquely named supplied files; no arbitrary file/network fetch. Cross-file derivatives share root identities; missing files and cycles are unresolved. This does NOT identify formula-free copying or establish authenticity/statistical independence.
- Folder CLI: `.venv/bin/python lab.py workbench analyze /path/to/folder`. User launcher: `Analyze-Workbook-Folder.command`. Public linked pair report: `examples/linked-workbooks/index.html`; five-file report: `examples/workbook-folder/index.html` (616/1338 formulas resolved, 722 explicitly excluded). Real engineering fixtures from Apache POI, not client audit workpapers.
- Current dependency tests: 26 pass. Two current report DOM checks pass under jsdom. Proof/source hashes: `restoration/20260914-workbook-bundle/validation.json`. Real Chrome visual verification has not been completed.
- R207 is actually called, but the same-batching AND/OR baseline beat it. Keep the negative result: `workbench_runs/20260913T233010Z-83f3db27-benchmark/receipt.json`. No new R207 advantage is demonstrated by external workbook support.

- Latest completed repair: REL now includes the no-alteration candidate and counts exact/reversed duplicate comparisons once; different thresholds on one pair require a joint model. Embedded REL copies in P017 RECS and P134 REIG also repaired. Their allocation/borrowing outputs are invariant to repeated checks. REL model weights remain uncalibrated and assume independence of distinct pairs/record priors.
- New `workbench/content.py` + content viewer: automatic literal block comparison alongside unchanged formula lineage. Formula-free workbooks now produce a report through the same `lab.py workbench analyze` command. Typed rare anchors, translation matches, 6 cells/3 values/80% density; no inferred provenance unions. This initial matcher only searched translations. The later record alignment below adds reordered rows/columns; transposition and same-sheet copies remain unsearched.
- Public observation: `examples/content-review-verified/index.html` finds 63/65 identical positions between EverythingTests!T7:X19 and FinanceLibTests!B2:F14 in Apache POI's file. The other two positions are literal 2/3 vs =2/3 formulas. Seven public fixtures are read. Not a real audit client sample.
- Current targeted proof: 55 tests (8 REL, 12 consumers, 35 workbook/content), `runs/20260914T154500Z-rel-content/receipt.json`. DOM interaction passes in `restoration/20260914-rel-content/ui-check.json`; not a real-browser visual check. A combined earlier test attempt failed at collection due to standalone REL import under importlib; final proof uses its successful separate 8-test run and successful 47-test run.
- Actual SimClone code/package was read: `research/simclone/OBSERVATIONS.md`. It injects clones and splits feature pairs randomly, not by source table; no replicated model result claimed. Narrower deterministic matcher is not a scientific breakthrough.
- `maintenance/catalog.py` had a real indentation error introduced in an earlier turn; fixed. Full catalog/explorer regeneration completed: 293 entries, all three repaired components point to the current successful receipt. Log: restoration/20260914-rel-content/catalog-build.log.

- Latest EBC repair: `HistoricalEstimate.observation_id` identifies one underlying normal estimate. Exact declared aliases count once (maximum allowed power); matching numbers alone do not merge. `current_observation_id` excludes the current observation from history. Different estimates/SE under one identity are rejected as requiring an overlap model. Partial covariance and nominal coverage remain unresolved.
- Embedded EBC copies repaired in P004 BAMI and P134 REIG. REIG coalesces identity before BOTH REL and EBC, preserving graph/prior and borrowing invariance. This goes beyond dropping duplicates only at the final averaging step.
- New CLI: `.venv/bin/python lab.py borrow-evidence examples/borrowing/linked-estimates.json --review`. Interactive engine-output example: `examples/borrowing/index.html`. Synthetic example shifts 0.472→0.667 if five aliases are assumed independent; declared-identity result stays 0.472 from 1 to 20 copies. No audit/fraud probability claimed.
- Latest targeted tests: 34 pass (EBC 11, BAMI 8, REIG 15 incl its separate unittest file), with closed-form and GLS perfect-duplicate reference. Receipt `runs/20260914T170000Z-ebc/receipt.json`; DOM check `restoration/20260914-ebc/ui-check.json`. Originals preserved under `restoration/20260914-ebc/before`. Package manifest updated only for changed files. Full catalog/explorer regeneration passed with 293 entries; EBC, BAMI and REIG point to the new receipt.

- New joint-covariance EBC API: `borrow_correlated_evidence` in R026/correlated.py, exposed by ebc.py. Explicit named covariance includes current/history dependence, with conditional likelihood z=h-(c/v)*current and design a=1-c/v. Diagonal matrices reduce to EBC; unit powers/no active cap reduce to GLS. `source_covariance` propagates A Sigma A.T. No inferred covariance from content similarity.
- BAMI and REIG accept `covariance`/`covariance_names` and propagate them to both borrowing calculations. Exact aliases still coalesce before REL and EBC; their covariance rows must match. General dependence among distinct REL pair comparisons remains unsolved. Different estimands cannot be pooled merely because they share a source.
- Latest 49 tests pass (R026 23, BAMI 9, REIG 17). Receipt `runs/20260914T190000Z-correlated-ebc/receipt.json`; canonical full test directory and both full consumer directories tested. Full catalog/explorer rebuilt: 293 entries, current receipt on these components.
- External numerical reproduction (not audit workpapers): `examples/correlated-evidence/report/index.html`, receipt.json and cli-result.json. 56-school dataset + published rounded fixed variance components reproduce joint estimate 0.18472238 / SE 0.08457226; ignoring covariance 0.12759697 / SE 0.04580421. Independent inverse-matrix GLS agrees. No REML fitting or adaptive coverage proof. Numeric data transcribed from official HTML table after direct downloads failed DNS; acquisition and sources recorded.
- Shared-control public counts also verify linear source propagation: contrast difference variance 0.131091924 vs incorrect diagonal-only 0.244187162. Distinct treatment effects not pooled. API/CLI agree; jsdom model selector passes. Reproduce via `.venv/bin/python examples/correlated-evidence/reproduce.py --output NEW_FOLDER`.
- Original EBC CLI still works; with covariance in input, `lab.py borrow-evidence INPUT --review` now contrasts supplied joint covariance against its diagonal under identical policies. New source module/test and embedded copies are in package manifest; original archives/releases untouched.

Remaining goal work includes demonstrating meaningful user benefit against strong baselines on independently sourced tasks, addressing evidence dependency beyond explicit spreadsheet links without turning similarity into invented provenance, and continuing substantive repairs/assessment of other valuable lab mechanisms. The scientific/product claims of the whole lab remain unverified. Do not equate the 82 targeted passing tests above with a fresh whole-lab test run.

Interrupted processes from earlier work are not evidence of activity: revalidate any tool handle before waiting. The previous all-source hashing run left `runs/20260913T234957Z-0dea6361/` empty; it did not produce a successful receipt. Targeted tests completed separately as recorded above. Some local dependency-file reads were very slow; completed native calculations ran normally afterward. Do not restart old broad checks without a new reason.

Latest completed workbook extension: `workbench/record_matches.py` maps columns using observed typed value sets, then verifies mutually unique row agreement. It does not treat equal marginal bags as copied rows. Content schema 2 combines this with translations, with bounded output. Ambiguous/unmatched row numbers and columns are visible. Reports open on differences and attach `difference_formula_targets` from existing R207 lineage; content matches never merge roots.

Current proof: `restoration/20260914-row-matching/validation.json`: 40 workbook/content tests, three jsdom report interactions, five public-data transformation tasks. Source patch and pre-change files alongside it. Original archives and prior reports retained. `examples/public-spending/verified/index.html` is the current public benchmark. Source CSV genuinely downloaded from HM Treasury, 84 rows, preserved bytes/hash/license. Excel row/column reordering, renamed headers, +123.45 change and SUM formulas are explicitly artificial; CSV parsing removes mixed £/thousands formatting, preserves other fields as text. App accepts XLSX/XLSM, not raw CSV.

Results: intact permutation 84/84 for all methods; changed unique voucher new matcher 84/84 vs exact rows 83/84 and header-fuzzy 84/84; changed repeated voucher all 83/84 and changed record omitted; renamed headers new matcher 84/84, header baselines inapplicable; independently permuted columns no record candidate. No generic superiority or field accuracy claimed. Current example report shows derived C37=42305.42 vs published H8=42181.97 and both Review total!A2 formula impacts. Ambiguous example identifies derived row 80 / published row 2 as unmapped. Formula root identities stay separate.

Reproduce: `.venv/bin/python examples/public-spending/reproduce.py --output NEW_FOLDER`. Same ordinary product CLI works on its inputs. Real Chrome visual verification is still incomplete. Full catalog/explorer rebuilt successfully, retaining 293 entries and previous engine receipts; main page links the new observations. Goal remains active: independently observed practical value and broader lab product restoration are not complete.

Latest completed native motor repair: AICC now integrates Gaussian max-affine utility analytically via the established knowledge-gradient envelope identity. The former 31-node calculation was 0.3883595356 vs closed form 0.3989422804, reversing measurement choice at cost 0.393. `quadrature_points` stays accepted but no longer affects values. Three embedded AICC copies (P136 QAIG, P138 SPIA, P144 DPAI) updated byte-identically; QAIG cost-boundary regression added.

AICC also supports explicit `channel_noise_covariance` + exact ordered `covariance_channel_names`. Optional finite mode treats each channel as one observation; square-root augmented Gaussian conditioning updates all remaining channel predictions. Exact shared-error copies add zero value/replay is idempotent, independent equal-valued channels still add information, partial positive/negative dependence matches batch conditioning. Default mode remains fresh independent measurement on every call. Covariance is not inferred from workbook similarity. Initially independent state/error and affine utilities remain assumptions. Normal-loss extreme-tail values can underflow; no log-value API or global multi-step optimality.

Actual upstream MATLAB code was downloaded/read (not executed): `research/aicc-knowledge-gradient/`, MIT license and hashes. `OBSERVATIONS.md` identifies the old numerical defect, known reference algorithm, and finite model semantics. Fresh proof: 36 tests (AICC17, QAIG7, SPIA6, DPAI6), `runs/20260914T191000Z-aicc/receipt.json`. Initial tests paused on imports but FINISHED (17+18); final reruns after code/new QAIG test passed quickly (17+19). All old handles terminal. Source originals, patch and touched-manifest update under `restoration/20260914-aicc`.

New ordinary command: `.venv/bin/python lab.py select-information examples/information-selection/input.json [--output NEW.json]`. The actual CLI output and static explanatory report are in `examples/information-selection/`. Example mean/variance after original .5/.5; after copy unchanged; after independent 2/3,1/3. These are synthetic model observations, not real audit recommendations. Parent test compares independent numerical integration and batch Gaussian reference; original archives intact. Broad goal remains active; meaningful user benefit beyond controlled examples and other promising lab mechanisms still need work.

AICC catalog/explorer rebuild completed successfully: 293 entries; AICC/QAIG/SPIA/DPAI point to the new 36-test receipt. Main page links the information-selection report and the spending-record comparison.

Latest completed product: P083 HEAG changed from generic score-only spec_runtime to a new native EBC → AICC composition. Actual historical code and old six tests remain missing; HISTORICAL_PRODUCT_RESULT.md unchanged and not inherited. Old source/three metadata tests are saved under restoration/20260914-heag/before. Canonical parent code is loaded, not copied. Parent EBC is loaded as a private package so .correlated/.ebc imports share one module identity.

New command: `.venv/bin/python lab.py evidence-acquisition examples/evidence-acquisition/verified/declared_copies-input.json --review [--output NEW.json]`. Input has evidence EBC configuration + scalar decision model with named affine actions, future channels/costs and explicit `future_error_relation: independent_of_evidence`. Shared future error with used evidence is rejected as requiring a larger joint model. EBC mean and squared SE are an explicit working Gaussian, not proved calibrated posterior. One next acquisition only; no legacy score/budget allocator.

Fresh 13 product tests pass: parent call instrumentation, copy/current identity invariance, independent equal estimates, conflict/cap ablations, covariance to selection vs direct GLS/closed form, costs/utilities, invalid adapters, nonmutating sensitivity comparison. Receipt `runs/20260914T192829Z-heag/receipt.json`; source changes, patch, UI and manifest-before in `restoration/20260914-heag`. No parent source changed this turn, so previous parent receipts remain valid at their tested scope.

Interactive current result: `examples/evidence-acquisition/verified/index.html`. Its cost slider compares current-only, dependency-preserved and independence-assumed choices. Five aliases: gross info value .162867504 vs .115164716; at cost .15 only dependency-preserved chooses the independent future measurement. All 1–20 aliases checked. Public56 clustered estimates reuse prior published fixed-covariance input, full powers/no active cap; hypothetical threshold .2 and future SE .1. Gross value .0149954414 vs 3.30579145e-7; independent GLS + two-action closed form agree. First row marked current is computational, not chronological. Model utility is hypothetical, not an audit/education recommendation. CLI and direct API agree.

HEAG UI jsdom passed (cost choices, dataset switch, small positive threshold, links). Browser bridge status was actually tried and returned MCP SSE 404; no real-browser visual verification claimed. `examples/evidence-acquisition/report` preserved, `verified` current. Generic Foundry count now89, new native count2; no historical code recovered by relabeling. Broader goal remains active: real-file auditor utility beyond controlled examples and further valuable lab mechanisms still require work.

Latest completed workbook repair: saved structured table references now resolve through workbench/table_references.py: table data/header/total/current-row selectors, escaped column names, adjacent column ranges and containing-table inference. Workbook identity stays separate. Missing/unsupported metadata, non-data #This Row and downstream errors remain unresolved; no numerical formula evaluation or inferred provenance. Static potential lineage may be conservative for implicit intersection without @.

Proof: restoration/20260915-structured-tables/validation.json, 69 passing tests across table/workbook/content/record suites and one jsdom source-toggle interaction. Two unmodified Apache POI fixtures improve from no usable formula graph to 9/9 and 1/1 formulas. The third larger workbook still fails the unchanged 20,000-populated-cell bound. Source code and expected-range tests from Apache were downloaded/read, not executed as a Java oracle. Downloads, licenses, hashes and behavior differences recorded under research/structured-references and examples/public-tables. Current usable report: examples/public-tables/StructuredReferences-report/lineage.html. Original archives/prior reports preserved. No LAC package source changed in this extension, so package manifest unchanged.
