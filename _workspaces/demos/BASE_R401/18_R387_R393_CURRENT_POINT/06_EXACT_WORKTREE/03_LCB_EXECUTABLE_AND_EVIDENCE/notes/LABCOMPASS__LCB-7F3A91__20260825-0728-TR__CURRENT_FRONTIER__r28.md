# LABCOMPASS LCB-7F3A91 current frontier r28 — VALUE-FIRST

- Provenance remains **455/455**.
- Curated queue remains **211** rows.
- Value-first objective remains binding: real economic/social utility and measured performance dominate novelty.
- Four high-value batch candidates were newly adjudicated:
  - `RX-033`: load-sharing liquidity mechanism confirmed but moderate in the main shell; keep as component.
  - `RX-037`: strong shell-gated survivor; matched current state AUC 0.5581 -> 0.8838 and log-loss 0.6853 -> 0.4361.
  - `RX-013`: linkage uncertainty propagation reduces RMSE 17.55–20.68% under ambiguity, but worsens MAE; downstream-loss gate required.
  - `RX-042`: rare-path importance sampling reduces extreme-tail estimator RMSE by ~828x at matched compute; non-rare gate switches off tilt.
- `RX-037` r01 is preserved as a negative result: when current telemetry already reveals damage, pipeline history adds essentially no value.
- Executable prototype library: **26 top-level classes** in r17.
- Binding next batch begins with `RX-070 MotifErrorLocator`; implement a real color-coding DP/meet-in-the-middle baseline rather than compare against a strawman DFS.
