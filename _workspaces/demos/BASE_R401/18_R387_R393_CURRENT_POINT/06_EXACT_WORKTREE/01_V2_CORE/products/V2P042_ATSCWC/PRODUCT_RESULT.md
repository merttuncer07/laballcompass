# V2P042 ATSCWC — Active-Threshold Sensing Calibration Width Controller

ActiveThresholdSensingV0 changes the measurement locations that generate the posterior center/scale stream consumed by CWC. The mechanism-removing comparator keeps the identical likelihood, query budget, response draws, and CWC but freezes the query schedule. In the declared synthetic shell the active composition preserves target coverage while producing narrower calibrated intervals and lower interval score.

Status: SHADOW_RETRO_SWEEP. This is finite synthetic mechanism evidence; response accuracy and task distribution are declared.
