# Recovered Negative Knowledge — R394 to R398

This is a high-value but non-exhaustive textual recovery of pair-local adjudications explicitly mentioned in the conversation. The original ephemeral JSON disposition files for late releases were lost in the runtime remount; do not pretend this list is exhaustive.

## Full/deep-family collisions
- `SACPS → AICC`: already represented by the covariance-aware acquisition path in V2P010; not a new family.
- `SGOT/GSSA → CAPL`: collapses toward the support-gated/weighted CAPL family (V2P053).
- `RECS/FPSD → CAPL`: safeguard/risk→heterogeneous-cap operator collapses toward V2P052.
- `P090 SWDL → DLEW`: full-mechanism collision because SWDL already contains OWS + DLEW.
- `RECS → DLEW`: weighted-DLEW family collision.
- `SACPS → DRE`: covariance-aware acquisition family collision (V2P010 neighborhood).
- `SWDL → AICC`: DLEW/AICC model-selection/value-of-information family collision after V2P055.

## Invalid or missing semantic bridges
- `SACPS → MIFF`: covariance cannot be promoted to directed information flow without extra causal/structural authority.
- `LCB-K043 → FPSD`: randomization p-value is not a failure probability.
- `LCB-K043 → REIS`: randomization p-value is not posterior culprit probability.
- `AICC → SACPS`: measurement-channel output does not natively define return/covariance observations.
- `DHCC → MIFF`: hedge exposure is not directed information flow.
- `DCBT → selection-aware reference law`: compression-budget tipping does not supply post-selection inference authority.
- `SACPS → BPISD`: no native bridge from covariance coordinates to persuasion prior/payoff uncertainty.
- `BMDT → selection-aware interval/reference`: tipping distance does not authorize confidence/family-size changes.
- `MIFF → FPSD`, `BMDT → MFPA`, `TWMR → DHCC`, `SGOT → CDBL`: rejected in R398 wave2 for missing native semantic/interface bridges.
- `AICC → DSBC`: Gaussian state mean/covariance and DSBC discrete simplex beliefs are different representations; softmax/normalization would invent authority.

## Method rule
These dispositions are pair-local. Do not mark either component globally dead. Preserve them so a future native router does not waste work rediscovering the same invalid bridge or family collision.
