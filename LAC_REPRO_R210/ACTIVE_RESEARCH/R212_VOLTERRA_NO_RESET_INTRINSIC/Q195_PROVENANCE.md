# Q195 Provenance (Step 17)

## ORIGINAL UNIONALPHA WORK RECOVERED FROM CHAT

From accessible history (transcript file, 18 blocks):
1. F* grid-surface enumerations d=3,5 (Block 1).
2. State support surveys showing monotonic growth (Blocks 2-3).
3. h/q prefix-time chronology construction (Blocks 4-6).
4. Joint physical kernel test, buggy (nullity 2) and fixed (nullity 0),
   d=3..15, n0=1,2 (Blocks 7-9).
5. P-mislabel diagnosis: P = h_{d-1} = R_d, duplicate-time artifact
   (Block 8).
6. Edge-vs-bin census d=3,5,7 (Block 10).
7. All-pairwise enlarged-span control with nullities 6/30/72/132
   (Block 11); fwd-only rank H-1 (Block 12).
8. Formal edge-space transversality, shapes to (1122,28) (Block 13).
9. Helical gap-label map, d=5 step sequence (Block 16).
10. d=5 closed 16-prefix grid walk (Block 17).
11. d=5 unsigned/signed full-cycle histograms (Block 18).

## PERSISTED BEFORE RECOVERY

R212_STATE.md (verdicts, bug note); q195_suffix_lift.py (physical_word,
suffix/affine rows, unfinished helical_label stub); cert file
(schedule_L3, states_int); S1, Q193/Q194 sources; paper draft, verify
scripts, repair bundle. See Q195_RECOVERY_SOURCE_INVENTORY.md.

## RECONSTRUCTED BY MUSE

q195_collision_recovered.py: surface_chronology, tildeQ, forward_code,
J_vec, pi_row, pi_vec, build_case, negative_control_enlarged_code,
edge_bin_stats, gap_label, integrate_walk, edge_histograms,
c_delta_representation; physical_word mirror of the filed function.
run_q195_recovery.py: all 12 replay checks incl.
test_historical_P_mislabel_bug. Every reconstructed section is commented.

## RECOMPUTED OUTPUT

Full sweep table (Q195_RECOVERED_SWEEP.txt, incl. previously unrecorded T
vectors); formal/transversality/non-vacuousness/negative-control numbers
(all match chat); width-3 labels/walk/histograms (exact match); c_delta
regroup identity (new representation check). Cells d=15/n0=2 and formal
d=13,15 extend beyond chat's run range via the same code path.

## NOT RECOVERED

1. Any same-chat UnionAlpha work predating the accessible scope
   ("What did we do so far?" onward is covered; anything earlier is
   invisible from here).
2. The corrected T-vectors as originally printed output (never printed in
   chat; recomputed, not transcribed).
3. The exact original heredoc characters (transcript is best-effort
   verbatim; functional fidelity is validated by replay, not by diff).
4. A source-level edge formula for Gamma[a,b] (the source never writes
   one; suffix-difference is reconstruction).
5. The filed R_0 = 1 initialization as a source statement (assumption,
   flagged by the R212 audit).
6. The width-3 transfer law c_{delta+1} = M_delta c_delta (never derived
   in accessible chat; only the c_delta representation is executable).

## MATHEMATICAL STATUS

Q195 general theorem: OPEN.
Finite exact sweeps are evidence, not proof. No general proof appears in
accessible UnionAlpha chat.
