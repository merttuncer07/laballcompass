# Q196 Three-Layer Involution (§§7, 9)

Y = x^d (Y^3 = −1); ω = ξ^d (ω^3 = 1). Every phase exponent splits as
a + dt (grid coords); S_{r−1} = Σ_a x^{−a} F_a(Y) with F_a(Y) in the
fixed 3-dim algebra span{1, Y, Y^2} (Y^3 = −1 reduction).

J in monomial grid coords (verified per cell): JG1 for a > 0,
Jx^{a+dt} = −ξ^{a+dt} x^{(d−a)+d(2−t)} (using r−(a+dt) = (d−a)+d(2−t));
JG2: JY^t = −ω^t Y^{3−t} (t = 1, 2); J1 = 1.

Dual grid moments M_{a,t} = λ⋆(x^{a+dt}); λ⋆∘J = λ⋆ becomes explicit
reflection relations among M_{a,t} (substitute JG1/JG2). No dense J
matrix is used past this point. These relations plus the transfer
measurement (no bounded affine law at tested widths) delimit the
verified dual framework.
