# Deployability-Aware Resolution Allocator

**Status:** PROMOTED v0.1

**Composition:** CDBL + ACRA

Uses CDBL deadline-specific lost deployable quantity as ACRA consequence weights so sensing resolution follows operational service loss rather than nominal stock/capacity.

## Validation

See `BENCHMARK_RESULT.json` and the dedicated unit tests. Claims are limited by the implementation contract.
