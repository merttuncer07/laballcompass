# Recovered Reference Code — NOT CANONICAL BYTES

These modules reconstruct the core R394–R398 operators from the surviving mechanism contracts and receipts. They are deliberately placed outside the exact worktree. They are intended to help a successor rebuild the lost ephemeral product trees against the exact parent code under `18_R387_R393_CURRENT_POINT/06_EXACT_WORKTREE`.

Each module has a small executable contract test. Passing these tests proves only that the recovery sketch preserves the stated operator/collapse/failure semantics; it does not replace the original hardened product tests or promotion receipts.
